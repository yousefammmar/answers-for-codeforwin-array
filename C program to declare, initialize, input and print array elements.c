//C program to declare, initialize, input and print array elements
#include <stdio.h>
int main(){
    int n,x,array[x];
    int i=0;
    printf("input size:");
    scanf("%d",&x);
    printf("input element");
    scanf("%d",&n);
    while(i<=(x-1)){
        printf("output:%d\n",n);
        n++;
        i++;
    }
    return 0;
}